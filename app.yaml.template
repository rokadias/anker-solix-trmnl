service: anker-solix-trmnl
runtime: python313
entrypoint: gunicorn -b :$PORT main:create_app --worker-class aiohttp.worker.GunicornWebWorker
instance_class: F1

env_variables:
  ANKERUSER: "{FILL_ME_OUT}"
  ANKERPASSWORD: "{FILL_ME_OUT}"
  ANKERCOUNTRY: "us"
  TRML_PLUGIN_UUID: "{FILL_ME_OUT}"
